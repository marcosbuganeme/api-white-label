###############################################################################
# API MaisVendas - Docker Compose Production Override
#
# Uso:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#   make prod-up
#
# Otimizado para: 4GB RAM / 2 vCPU (budget total: ~2,944M / 3.1 CPU)
###############################################################################

services:
  # ──────────────────────────────────────────────
  # DOCKER SOCKET PROXY - Limits Docker API access for Traefik
  # ──────────────────────────────────────────────
  docker-socket-proxy:
    restart: always
    deploy:
      resources:
        limits:
          memory: 32M
          cpus: '0.05'

  # ──────────────────────────────────────────────
  # EDGE PROXY - Traefik
  # ──────────────────────────────────────────────
  traefik:
    restart: always
    volumes:
      - ./docker/traefik/traefik.prod.yml:/etc/traefik/traefik.yml:ro
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: '0.15'

  # ──────────────────────────────────────────────
  # WEB SERVER - Nginx
  # ──────────────────────────────────────────────
  nginx:
    restart: always
    # Public assets are baked into the nginx image (COPY public) - no volume needed.
    # This prevents stale assets from persisting across deploys via named volumes.
    labels:
      # Traefik health-aware routing: only send traffic when backend is healthy
      - "traefik.http.services.api.loadbalancer.healthCheck.path=/v1/health/live"
      - "traefik.http.services.api.loadbalancer.healthCheck.interval=5s"
      - "traefik.http.services.api.loadbalancer.healthCheck.timeout=3s"
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: '0.15'

  # ──────────────────────────────────────────────
  # APPLICATION - PHP-FPM
  # ──────────────────────────────────────────────
  app:
    restart: always
    build:
      target: production
    environment:
      APP_ENV: production
    volumes:
      - app_storage:/var/www/html/storage
    deploy:
      resources:
        limits:
          memory: 768M
          cpus: '0.75'

  # ──────────────────────────────────────────────
  # HORIZON - Redis Queue Manager
  # ──────────────────────────────────────────────
  horizon:
    restart: always
    build:
      target: production
    environment:
      APP_ENV: production
    volumes:
      - app_storage:/var/www/html/storage
    stop_grace_period: 30s
    deploy:
      resources:
        limits:
          memory: 192M
          cpus: '0.30'

  # ──────────────────────────────────────────────
  # RABBITMQ WORKER - Heavy Processing
  # ──────────────────────────────────────────────
  rabbitmq-worker:
    restart: always
    build:
      target: production
    command: >
      php artisan queue:work rabbitmq
      --queue=processing,data_crossref,reports
      --max-jobs=1000
      --max-time=3600
      --memory=90
      --tries=3
      --backoff=30,60,120
      --sleep=3
    environment:
      APP_ENV: production
    volumes:
      - app_storage:/var/www/html/storage
    stop_grace_period: 60s
    deploy:
      resources:
        limits:
          memory: 192M
          cpus: '0.30'

  # ──────────────────────────────────────────────
  # SCHEDULER - Laravel Schedule
  # ──────────────────────────────────────────────
  scheduler:
    restart: always
    build:
      target: production
    environment:
      APP_ENV: production
    volumes:
      - app_storage:/var/www/html/storage
    stop_grace_period: 15s
    deploy:
      resources:
        limits:
          memory: 192M
          cpus: '0.25'

  # ──────────────────────────────────────────────
  # PostgreSQL - Optimized for 640M limit
  # ──────────────────────────────────────────────
  postgres:
    restart: always
    ports: !reset []
    command: >
      postgres
        -c shared_buffers=128MB
        -c effective_cache_size=384MB
        -c work_mem=4MB
        -c maintenance_work_mem=64MB
        -c max_connections=50
        -c log_min_duration_statement=200
        -c log_checkpoints=on
        -c log_connections=on
        -c log_disconnections=on
    deploy:
      resources:
        limits:
          memory: 640M
          cpus: '0.50'

  # ──────────────────────────────────────────────
  # Redis - Optimized for 256M limit
  # ──────────────────────────────────────────────
  redis:
    restart: always
    ports: !reset []
    command: >
      redis-server
        --appendonly yes
        --maxmemory 128mb
        --maxmemory-policy allkeys-lru
        --tcp-backlog 511
        --timeout 300
        --tcp-keepalive 60
        --save 3600 1
        --save 300 10
        --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.15'

  # ──────────────────────────────────────────────
  # MongoDB - WiredTiger cache 256MB
  # ──────────────────────────────────────────────
  mongodb:
    restart: always
    ports: !reset []
    command: >
      mongod
        --wiredTigerCacheSizeGB 0.25
        --auth
        --bind_ip_all
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.30'

  # ──────────────────────────────────────────────
  # RabbitMQ - Watermark 0.7
  # ──────────────────────────────────────────────
  rabbitmq:
    restart: always
    ports: !reset []
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: '0.25'

  # ──────────────────────────────────────────────
  # ALLOY - Grafana Observability Agent
  # ──────────────────────────────────────────────
  alloy:
    restart: always
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: '0.10'

volumes:
  app_storage:
    driver: local
