###############################################################################
# API MaisVendas - Docker Compose (Development)
#
# Arquitetura:
#   Traefik (edge proxy) -> Nginx (web server) -> PHP-FPM (app)
#                                                  +-- PostgreSQL (CRUD)
#                                                  +-- Redis (cache/sessions)
#                                                  +-- MongoDB (logs/metricas)
#                                                  +-- RabbitMQ (heavy queues)
#
# Uso:
#   docker compose up -d            # Subir tudo
#   docker compose up -d --scale rabbitmq-worker=3  # Escalar workers
###############################################################################

services:
  # ──────────────────────────────────────────────
  # EDGE PROXY - Traefik
  # ──────────────────────────────────────────────
  traefik:
    image: traefik:v3.3.7
    container_name: maisvendas-traefik
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "${TRAEFIK_HTTP_PORT:-80}:80"
      - "${TRAEFIK_HTTPS_PORT:-443}:443"
      - "127.0.0.1:${TRAEFIK_DASHBOARD_PORT:-8080}:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./docker/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./docker/traefik/dynamic:/etc/traefik/dynamic:ro
      - traefik_letsencrypt:/letsencrypt
    networks:
      - maisvendas
    labels:
      - "traefik.enable=true"

  # ──────────────────────────────────────────────
  # WEB SERVER - Nginx
  # ──────────────────────────────────────────────
  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    container_name: maisvendas-nginx
    restart: unless-stopped
    volumes:
      - ./public:/var/www/html/public:ro
      - ./docker/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      app:
        condition: service_healthy
    networks:
      - maisvendas
    labels:
      - "traefik.enable=true"
      # HTTP router (dev: serves directly, prod: redirects to HTTPS via middleware)
      - "traefik.http.routers.api.rule=Host(`${APP_DOMAIN:-api.maisvendas.localhost}`)"
      - "traefik.http.routers.api.entrypoints=web"
      - "traefik.http.routers.api.middlewares=api-redirect-chain@file"
      - "traefik.http.services.api.loadbalancer.server.port=80"
      # HTTPS router (production - uses api-chain with security headers)
      - "traefik.http.routers.api-secure.rule=Host(`${APP_DOMAIN:-api.maisvendas.localhost}`)"
      - "traefik.http.routers.api-secure.entrypoints=websecure"
      - "traefik.http.routers.api-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.api-secure.middlewares=api-chain@file"

  # ──────────────────────────────────────────────
  # APPLICATION - PHP-FPM
  # ──────────────────────────────────────────────
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
      target: ${DOCKER_TARGET:-development}
      args:
        WWWUSER: ${WWWUSER:-1000}
        WWWGROUP: ${WWWGROUP:-1000}
    container_name: maisvendas-app
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    volumes:
      - .:/var/www/html
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - maisvendas
    environment:
      APP_ENV: ${APP_ENV:-local}
      CONTAINER_ROLE: app
      PHP_FPM_PM_MAX_CHILDREN: ${PHP_FPM_PM_MAX_CHILDREN:-50}

  # ──────────────────────────────────────────────
  # HORIZON - Redis Queue Manager
  # ──────────────────────────────────────────────
  horizon:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
      target: ${DOCKER_TARGET:-development}
      args:
        WWWUSER: ${WWWUSER:-1000}
        WWWGROUP: ${WWWGROUP:-1000}
    container_name: maisvendas-horizon
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    command: php artisan horizon
    volumes:
      - .:/var/www/html:ro
      - ./storage:/var/www/html/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    networks:
      - maisvendas
    environment:
      APP_ENV: ${APP_ENV:-local}
      CONTAINER_ROLE: horizon
      XDEBUG_MODE: "off"
    healthcheck:
      test: ["CMD-SHELL", "php artisan horizon:status | grep -q running || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 15s
      retries: 3

  # ──────────────────────────────────────────────
  # RABBITMQ WORKER - Heavy Processing
  # ──────────────────────────────────────────────
  rabbitmq-worker:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
      target: ${DOCKER_TARGET:-development}
      args:
        WWWUSER: ${WWWUSER:-1000}
        WWWGROUP: ${WWWGROUP:-1000}
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    command: >
      php artisan queue:work rabbitmq
      --queue=processing,data_crossref,reports
      --max-jobs=1000
      --max-time=3600
      --memory=230
      --tries=3
      --backoff=30,60,120
      --sleep=3
    volumes:
      - .:/var/www/html:ro
      - ./storage:/var/www/html/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - maisvendas
    environment:
      APP_ENV: ${APP_ENV:-local}
      CONTAINER_ROLE: rabbitmq-worker
      XDEBUG_MODE: "off"
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f 'queue:work rabbitmq' > /dev/null || exit 1"]
      interval: 60s
      timeout: 10s
      start_period: 15s
      retries: 3

  # ──────────────────────────────────────────────
  # SCHEDULER - Laravel Schedule
  # ──────────────────────────────────────────────
  scheduler:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
      target: ${DOCKER_TARGET:-development}
      args:
        WWWUSER: ${WWWUSER:-1000}
        WWWGROUP: ${WWWGROUP:-1000}
    container_name: maisvendas-scheduler
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    command: php artisan schedule:work
    volumes:
      - .:/var/www/html:ro
      - ./storage:/var/www/html/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f 'schedule:work' > /dev/null || exit 1"]
      interval: 60s
      timeout: 5s
      start_period: 10s
      retries: 3
    networks:
      - maisvendas
    environment:
      APP_ENV: ${APP_ENV:-local}
      CONTAINER_ROLE: scheduler
      XDEBUG_MODE: "off"

  # ==================================================
  # DATA STORES
  # ==================================================

  # ──────────────────────────────────────────────
  # PostgreSQL - Primary Relational Database
  # ──────────────────────────────────────────────
  postgres:
    image: postgres:17.9-alpine
    container_name: maisvendas-postgres
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "127.0.0.1:${DB_EXTERNAL_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: ${DB_DATABASE:-maisvendas}
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    networks:
      - maisvendas
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres} -d ${DB_DATABASE:-maisvendas}"]
      interval: 5s
      timeout: 5s
      start_period: 30s
      retries: 5
    command: >
      postgres
        -c shared_buffers=256MB
        -c effective_cache_size=768MB
        -c work_mem=16MB
        -c maintenance_work_mem=128MB
        -c max_connections=200
        -c log_min_duration_statement=200
        -c log_checkpoints=on
        -c log_connections=on
        -c log_disconnections=on

  # ──────────────────────────────────────────────
  # Redis - Cache, Sessions, Light Queues
  # ──────────────────────────────────────────────
  redis:
    image: redis:7.4.8-alpine
    container_name: maisvendas-redis
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "127.0.0.1:${REDIS_EXTERNAL_PORT:-6379}:6379"
    command: >
      redis-server
        --appendonly yes
        --maxmemory 512mb
        --maxmemory-policy allkeys-lru
        --tcp-backlog 511
        --timeout 300
        --tcp-keepalive 60
        --save 60 1000
        --save 300 100
        --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    volumes:
      - redis_data:/data
    networks:
      - maisvendas
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $$REDIS_PASSWORD ping | grep -q PONG"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ──────────────────────────────────────────────
  # MongoDB - Logs, Metrics, Processed Data
  # ──────────────────────────────────────────────
  mongodb:
    image: mongo:8.0.19
    container_name: maisvendas-mongodb
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "127.0.0.1:${MONGO_EXTERNAL_PORT:-27017}:27017"
    environment:
      MONGO_INITDB_DATABASE: ${MONGODB_DATABASE:-maisvendas_logs}
      MONGO_INITDB_ROOT_USERNAME: ${MONGODB_USER:?MONGODB_USER is required}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGODB_PASSWORD:?MONGODB_PASSWORD is required}
    volumes:
      - mongodb_data:/data/db
      - ./docker/mongodb/init:/docker-entrypoint-initdb.d:ro
    networks:
      - maisvendas
    healthcheck:
      test: ["CMD-SHELL", "mongosh -u \"$$MONGO_INITDB_ROOT_USERNAME\" -p \"$$MONGO_INITDB_ROOT_PASSWORD\" --authenticationDatabase admin --eval \"db.adminCommand('ping')\" --quiet"]
      interval: 10s
      timeout: 5s
      start_period: 30s
      retries: 5

  # ──────────────────────────────────────────────
  # RabbitMQ - Message Broker (Heavy Processing)
  # ──────────────────────────────────────────────
  rabbitmq:
    image: rabbitmq:4.2.4-management-alpine
    container_name: maisvendas-rabbitmq
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "127.0.0.1:${RABBITMQ_PORT:-5672}:5672"
      - "127.0.0.1:${RABBITMQ_MANAGEMENT_PORT:-15672}:15672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:?RABBITMQ_USER is required}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:?RABBITMQ_PASSWORD is required}
      RABBITMQ_DEFAULT_VHOST: ${RABBITMQ_VHOST:-maisvendas}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
      - ./docker/rabbitmq/definitions.json:/etc/rabbitmq/definitions.json:ro
      - ./docker/rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
    networks:
      - maisvendas
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 10s
      retries: 5
    labels:
      - "traefik.enable=true"
      # BasicAuth middleware defined via Docker label (env var interpolation works here, unlike file provider)
      # Generate hash: htpasswd -nB admin
      - "traefik.http.middlewares.rabbitmq-auth.basicauth.users=${RABBITMQ_MGMT_HTPASSWD}"
      # HTTP router (dev only)
      - "traefik.http.routers.rabbitmq.rule=Host(`rabbitmq.maisvendas.localhost`)"
      - "traefik.http.routers.rabbitmq.entrypoints=web"
      - "traefik.http.routers.rabbitmq.middlewares=rabbitmq-auth"
      - "traefik.http.services.rabbitmq.loadbalancer.server.port=15672"
      # HTTPS router (production - redirect HTTP to HTTPS + TLS)
      - "traefik.http.routers.rabbitmq-secure.rule=Host(`rabbitmq.${APP_DOMAIN:-maisvendas.localhost}`)"
      - "traefik.http.routers.rabbitmq-secure.entrypoints=websecure"
      - "traefik.http.routers.rabbitmq-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.rabbitmq-secure.middlewares=rabbitmq-auth"

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  mongodb_data:
    driver: local
  rabbitmq_data:
    driver: local
  traefik_letsencrypt:
    driver: local

networks:
  maisvendas:
    driver: bridge
    name: maisvendas
